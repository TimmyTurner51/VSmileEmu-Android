/**
 * Android Graphics Utilities
 * Helper functions for rendering (currently minimal - rendering is done in Kotlin/Compose)
 */

#include <android/log.h>

#define LOG_TAG "AndroidGraphics"
#define LOGI(...) __android_log_print(ANDROID_LOG_INFO, LOG_TAG, __VA_ARGS__)

// Graphics utilities would go here if needed
// Currently, we pass framebuffer directly to Kotlin/Compose for rendering






